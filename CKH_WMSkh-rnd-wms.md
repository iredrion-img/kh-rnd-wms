# WMS 테스트 서버 운영 가이드 (Q&A)

### 1. 현재 `http://localhost:3001`은 작업 PC에서만 접속되나요?
**네, 맞습니다.** 
`localhost`는 '내 컴퓨터(현재 작동 중인 PC)'를 의미하므로, 외부에서는 절대 접속할 수 없는 완벽하게 분리된 회원님만의 전용 공간입니다.

### 2. 테스트하며 저장/수정한 데이터는 실운영 WMS에 영향이 없나요?
**전혀 없습니다. 100% 분리되어 있습니다.**
현재 테스트 서버는 작업 PC(`C:\KH_WMS\kh-rnd-wms`) 안에 복사해 둔 JSON 파일들을 읽고 씁니다. 실운영 서버 PC의 파일들과 물리적으로 완전히 분리되어 있기 때문에 여기서 아무리 데이터를 지우거나 수정해도 운영 서버에는 티끌 하나 영향이 가지 않습니다. 안심하고 마음껏 테스트하셔도 됩니다!

### 3. 나중에 또 테스트하려면 브라우저만 켜면 되나요?
**아닙니다. 서버 프로그램을 먼저 켜주셔야 합니다.**
지금 띄워두신 검은색 터미널 창을 끄거나 컴퓨터를 재부팅하면 테스트 서버 프로그램도 종료됩니다.
나중에 다시 테스트하고 싶으실 때는:
1. 터미널을 열고 `C:\KH_WMS\kh-rnd-wms` 경로로 들어갑니다.
2. `node server.js` 명령어를 쳐서 서버를 켜줍니다.
3. 그 다음에 브라우저에서 `http://localhost:3001` 로 접속하시면 됩니다.

### 4. 실운영 서버의 '최신 데이터'를 테스트 서버로 가져오려면?
소스 코드는 그대로 두고, 사람들이 입력한 최신 내용(데이터)만 가져오고 싶으실 때의 방법입니다.
1. 실운영 PC의 `backups/` 폴더 등에 있는 최신 JSON 파일들을 복사해옵니다.
2. 작업 PC의 WMS 폴더 안에 덮어쓰기(붙여넣기) 합니다.
3. 터미널 창에서 돌고 있는 서버를 끄고(`Ctrl + C`), 다시 `node server.js`로 켭니다. (이것만 하면 끝이며 별도의 빌드 명령어는 필요 없습니다!)

### 5. 실운영 서버의 '최신 기능(소스코드)'을 테스트 서버로 가져오려면?
만약 실운영 서버에 먼저 새로운 기능 코드가 적용되었다면, 작업 PC(테스트 서버) 터미널에서 다음을 실행합니다.
```powershell
git fetch origin
git reset --hard origin/feature/weekly-meeting
npm run build
node server.js
```
*(이때, 깃허브를 통해 코드를 동기화하더라도 작업 PC에 저장해둔 테스트용 JSON 데이터들은 삭제되지 않고 그대로 유지됩니다.)*

### 6. 테스트 서버에서 만든 '새로운 기능'을 실운영 서버로 올리려면?
이것이 우리가 앞으로 작업할 **정석적이고 가장 안전한 개발 흐름**입니다!
1. **작업 PC(테스트 서버)**에서 저와 함께 새로운 기능을 만들고 테스트를 완료합니다.
2. 만족스럽게 개발이 끝나면, 제가 코드를 깃허브(GitHub)에 업로드(Push)해 둡니다.
3. **실운영 서버 PC** 터미널에서 아래 명령어를 쳐서 깃허브의 완성된 코드를 내려받습니다.
   ```powershell
   git fetch origin
   git reset --hard origin/feature/weekly-meeting
   npm run build
   ```
4. 운영 서버를 껐다 켜면, 기존 데이터는 100% 유지된 채로 새로운 기능만 운영 서버에 안전하게 적용됩니다!
