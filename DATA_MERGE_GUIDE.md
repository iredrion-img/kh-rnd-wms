# WMS 과거 데이터 병합(Merge) 및 관리 가이드

이 문서는 2026년 7월 기준으로 분리해 둔 과거 데이터(6월 이전)를 추후 현재 시스템에 다시 합치고 싶을 때 사용하는 방법에 대한 가이드입니다.

---

## 1. 사전 준비 사항
과거 데이터(JSON 파일들)가 들어있는 백업 폴더가 필요합니다. 
(예: `backups/2026-06-30`, `backups/archive_2026_06` 등)

해당 백업 폴더 안에는 다음 파일들이 포함되어 있어야 정상적으로 병합이 가능합니다.
* `database_2026.json` (타임시트)
* `weekly_tasks_2026.json` (주간 업무)
* `weekly_schedule.json` (주간 일정)
* `meeting_overview.json` (주간 회의록)

---

## 2. 데이터 병합 실행 방법

서버 PC에서 아래 순서대로 명령어를 실행해 주시면 됩니다.

1. **명령 프롬프트(CMD) 열기**
2. **WMS 서버 폴더로 이동**
   ```cmd
   cd c:\KH_WMS\kh-rnd-wms
   ```
3. **병합 스크립트 실행**
   `node merge_data.cjs [백업폴더경로]` 형식으로 입력합니다. 
   예를 들어, 백업 데이터가 `backups/2026-06-30` 폴더에 있다면 아래와 같이 입력합니다.
   ```cmd
   node merge_data.cjs backups/2026-06-30
   ```
4. **결과 확인**
   터미널에 `[병합완료] database_2026.json: OO개의 과거 데이터를 복구했습니다.` 등의 메시지가 뜨며 정상적으로 병합되었는지 확인합니다. (이미 존재하는 데이터는 중복을 피하기 위해 자동으로 건너뜁니다.)

---

## 3. 필수 마무리 작업 (서버 캐시 초기화)

데이터가 병합되어 파일에 저장되었더라도, 서버가 켜져있는 상태라면 기존 캐시 때문에 화면에 바로 나타나지 않을 수 있습니다. **반드시 서버를 재시작**하여 변경된 데이터를 웹 화면에 반영해야 합니다.

* **서버 재시작 명령어:**
  ```cmd
  pm2 restart wms-backend
  ```
  *(또는 바탕화면의 `Stop_Background.bat` 실행 후 `Start_Background.bat` 실행)*

서버 재시작 후 브라우저에서 대시보드를 새로고침 하시면 과거 데이터가 현재 데이터와 합쳐져 정상적으로 출력됩니다.

---

## 💡 참고: 데이터 삭제용 스크립트 (`cleanup_data.cjs`)
혹시 나중에 또 한 번 특정 시점 이전의 데이터를 날려야 할 일이 생긴다면, 폴더에 있는 `cleanup_data.cjs` 파일 안의 날짜(`2026-07-01` 등) 부분을 원하시는 기준일로 수정하신 뒤, 아래 명령어로 실행하시면 동일하게 과거 데이터를 삭제할 수 있습니다.
```cmd
node cleanup_data.cjs
```
(마찬가지로 삭제 작업 후에도 `pm2 restart wms-backend`를 잊지 마세요!)
