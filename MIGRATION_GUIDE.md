# 구글 시트 → WMS 주간공정회의 데이터 마이그레이션 가이드

기존에 구글 스프레드시트로 관리하시던 주간공정회의 및 부서별 업무 데이터를 신규 구축된 **KH R&D센터 WMS 시스템**으로 안전하고 정확하게 이전하기 위한 3가지 전략을 제안합니다. 센터의 현재 데이터 규모와 실무진의 편의성에 맞춰 가장 적합한 방식을 선택하실 수 있습니다.

---

## 📊 WMS 주간공정회의 데이터 구조 (Target Schema)

WMS 백엔드는 `weekly_tasks_YYYY.json` 파일에 아래와 같은 JSON 객체 배열 형태로 업무를 저장합니다. 구글 시트의 열(Column)들을 아래 항목에 매핑하여 이전하게 됩니다.

| WMS 필드명 | 데이터 타입 | 설명 및 구글 시트 매핑 예시 | 필수 여부 |
| :--- | :--- | :--- | :--- |
| **team** | `String` | 부서명 (예: `스마트 기술 개발팀`, `인프라 BIM팀`, `공통업무&행정`) | 필수 |
| **category** | `String` | 대분류 (예: 프로젝트, 연구과제, 부서지원 등) | 선택 |
| **sub_category** | `String` | 중분류 / 세부 항목 | 선택 |
| **task_code** | `String` | 과제 코드 (선택적 입력 항목) | 선택 |
| **content** | `String` | **주요 내용** (회의 핵심 안건 및 업무 내용) | **필수** |
| **assignees** | `String` | 담당자 (예: `김진희, 강수민` 또는 텍스트) | 필수 |
| **status** | `String` | 상태 (`진행 중`, `완료`, `보류` 등) | 기본값: `진행 중` |
| **priority** | `String` | 중요도 (`높음`, `중간`, `낮음`) | 기본값: `중간` |
| **start_date** | `String` | 업무 시작일 (`YYYY-MM-DD`) | 필수 (주차 계산용) |
| **end_date** | `String` | 업무 종료일 (`YYYY-MM-DD`) | 선택 |
| **meeting_result**| `String` | 회의 결과 / 조치 사항 | 선택 |
| **note** | `String` | 비고 | 선택 |
| **week_start** | `String` | 해당 업무가 속한 주차의 **월요일 날짜** (`YYYY-MM-DD`) | **자동 계산** |

---

## 🚀 추천 이전 방식 3가지

### 1️⃣ [추천] CSV/엑셀 변환 스크립트 실행 방식 (가장 빠르고 확실한 방법)
개발자나 관리자가 단 한 번의 조작으로 기존 수년/수개월치 데이터를 완벽하게 밀어넣는 방식입니다.
* **진행 절차**:
  1. 구글 시트에서 **[파일] → [다운로드] → [쉼표로 구분된 값(.csv)]** 또는 **[Excel(.xlsx)]**을 클릭하여 파일을 받습니다.
  2. 다운로드한 파일을 서버 컴퓨터의 WMS 폴더(예: `c:\KH_WMS\kh-rnd-wms\`)에 넣습니다.
  3. 저희가 미리 작성해 드리는 `migrate_google_sheets.js` 스크립트를 터미널에서 실행합니다.
     ```bash
     node migrate_google_sheets.js ./my_google_data.csv
     ```
  4. 스크립트가 날짜와 부서명을 WMS 표준에 맞게 자동 정규화(`normalize`)하여 DB 파일(`weekly_tasks_2026.json`)에 병합합니다.

> **장점**: 수천 건의 데이터도 수 초 내에 에러 없이 완벽하게 변환 및 병합됩니다.

---

### 2️⃣ 웹 화면(UI) 내 "엑셀 일괄 업로드" 기능 개발 방식
실무 담당자가 브라우저 화면에서 언제든 직접 엑셀 파일을 업로드할 수 있는 관리자용 기능을 추가하는 방식입니다.
* **진행 절차**:
  1. WMS 주간공정회의 페이지 상단에 **[엑셀 일괄 업로드]** 버튼을 신설합니다.
  2. 구글 시트 데이터를 엑셀로 내려받아 웹 화면에 드래그&드롭합니다.
  3. 웹 화면에서 표 형태로 업로드될 데이터를 미리 검토한 후 [서버에 저장] 버튼을 누르면 일괄 등록됩니다.

> **장점**: 터미널 조작이나 서버 접속 없이 누구나 손쉽게 잔여 데이터를 추가 등록할 수 있어 유지보수성이 높습니다.

---

### 3️⃣ 구글 스프레드시트 API 직접 연동 방식 (완전 자동화)
구글 클라우드 연동을 통해 파일 다운로드 행위조차 생략하는 방식입니다.
* **진행 절차**:
  1. 구글 시트 API 키(Service Account)를 발급받아 WMS 서버에 등록합니다.
  2. WMS 화면에서 **[구글 시트 가져오기]** 버튼을 누르면 지정된 구글 시트 주소에서 실시간으로 데이터를 긁어와 DB에 적재합니다.

> **장점**: 파일 이동 과정이 전혀 없어 가장 스마트하고 자동화된 느낌을 줍니다. (단, 초기 API 연동 설정이 다소 필요합니다.)

---

## 💡 다음 단계 제안 (Next Step)

가장 빠르고 깔끔한 이전을 원하신다면 **1번 방식(변환 스크립트)**을 추천해 드립니다. 
현재 구글 시트에서 사용 중이신 **실제 열(Column) 헤더 이름들(예: 부서, 주요내용, 담당자 등)** 또는 샘플 데이터 몇 줄을 텍스트로 공유해 주시면, 파일을 넣고 명령어 한 번만 치면 즉시 이전이 완료되는 **맞춤형 마이그레이션 스크립트**를 지금 바로 작성해 드리겠습니다!
